apiVersion: v1
kind: Service
metadata:
  name: payment-processor
  labels:
    app: payment-processor
spec:
  type: ClusterIP
  selector:
    app: payment-processor
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
